#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <map>

using namespace std;

map<string, int > m;

int main() {

    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int t, f;
    string s1, s2;

    cin >> t;
    while(t--) {
        cin >> f;

        while(f--) {
            cin >> s1 >> s2;
        }
    }




    return 0;
}