#include <stdio.h>
#include <vector>
#include <math.h>
#define ll long long

using namespace std;

int N, M, K;
vector<ll> l;

void update(int before, int after) {


}
void solve() {



}

int main() {

    ll tmp;
    int a, b, c;
    int times = M + K;

    scanf("%d %d %d", &N, &M, &K);
    for (int i = 0; i < N; i++) {
        scanf("%lld", tmp);
        l.push_back(tmp);
    }

    while(times--) {
        scanf("%d %d %d", &a, &b, &c);
        if (a == 1) {


        }
        else if (a == 2) {

        }
    }

    return 0;
}
