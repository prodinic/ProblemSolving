#include <iostream>
#include <algorithm>
#include <vector>
#include <queue>
#define p pair<int, int > 
#define pp pair<int, pair<int, int > >

using namespace std; 

queue<p> from_queue, to_queue;
priority_queue<pp> pq;
 
void bfs() {



}

void simulation() {

    // 1. Add initial points to from_queue
    // 2. Add adjacent points to to_queue
    // 3. While loop until from_queue is empty
    // 4. bfs() if from_point can reach out to to_point
    // 5. If it can't, dump every to_queue to from_queue 





}

int main() {








    return 0;
}