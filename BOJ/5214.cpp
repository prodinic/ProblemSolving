#include <iostream>
#include <algorithm>
#include <queue>
#define MAX_NUM 100001

using namespace std;

int N, M, K;
vector<vector<int > > v(MAX_NUM);

int main() {

    cin >> N >> M >> K;

    for (int i = 0; i < K; i++) {

        vector<int > tmp;
        for (int j = 0; j < M; j++) {
            int a;
            
            cin >> a;
            tmp.push_back(a);
        }

         
    }








    return 0;
}